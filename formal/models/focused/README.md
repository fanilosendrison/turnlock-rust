# Focused TLC configurations

Focused `.cfg` files will restrict the shared `Turnlock.tla` model for fast exploration and diagnosis of particular mechanisms or properties.

They are development accelerators only; they do not replace integrated exploration.
