# Integrated TLC configurations

Integrated `.cfg` files will exercise all currently formalized TURNLOCK core execution forms together within finite bounds.

`formal/verification.yaml` owns the current integrated profile set, paths,
lifecycle states and intents. This README documents only the role of the
directory.
